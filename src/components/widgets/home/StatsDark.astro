---
import WidgetWrapper from '~/components/ui/WidgetWrapper.astro';

import statsBgImage from '~/assets/images/pexels-eva-bronzini.png';

const { id, isDark = true, classes = {} } = Astro.props;

interface Stat {
  amount: string;
  title: string;
  description: string;
}

const stats: Stat[] = [
  {
    amount: '100%',
    title: 'ROI',
    description: 'Every $1 in UX returns up to $100 — proving design drives profitability.',
  },
  {
    amount: '65%',
    title: 'Engagement',
    description: 'AI engine drove 65% engagement lift for a top retailer, boosting repeat sales.',
  },
  {
    amount: '40%',
    title: 'Cost cut',
    description: 'Microservices cut costs 40% and boosted performance.',
  },
];
---

<WidgetWrapper id={id} isDark={isDark} containerClass={`relative ${classes?.container ?? ''}`}>
  <!-- Background: image + overlay (rendered in template so image import is resolved) -->
  <Fragment slot="bg">
    <div
      class="absolute w-full h-full inset-0 bg-cover bg-center bg-no-repeat"
      style={`background-image: url('${statsBgImage.src}');`}
    >
    </div>
    <div class="absolute inset-0"></div>
  </Fragment>

  <div class="relative z-10 flex flex-col md:flex-row md:items-center justify-between gap-6 mb-12 md:mb-28">
    <div class="md:w-1/2 mb-8 md:mb-0">
      <h2 class="text-6xl font-bold text-white">Facts</h2>
    </div>
    <div class="md:w-1/2 flex justify-start md:justify-end">
      <a
        href="/contact"
        class="inline-block px-8 py-3 rounded-full font-medium text-2xl border border-gray-500 text-white hover:bg-white hover:text-black transition-colors"
      >
        Get your free consultation
      </a>
    </div>
  </div>

  <div class="relative z-10 mb-20">
    <h3 class="text-3xl md:text-5xl text-gray-300 font-serif leading-tight">
      Empowering <span class="italic font-normal">startups & enterprises</span> with data-driven AI, engineering & design.
    </h3>
  </div>

  <div class="relative z-10 grid grid-cols-1 md:grid-cols-3 gap-16 md:gap-72 text-white mb-16">
    {
      stats.map((stat) => (
        <div class="text-left">
          <div class="text-7xl font-normal mb-4">{stat.amount}</div>
          <div class="text-6xl font-semibold font-serif italic text-white mb-4">{stat.title}</div>
          <p class="text-sm md:text-base text-white/90 leading-relaxed">{stat.description}</p>
        </div>
      ))
    }
  </div>
</WidgetWrapper>
